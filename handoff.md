# 3D 打印服务系统交接文档

> 生成日期：2026-09-06
>
> 交接目的：暂停继续实现，将当前对话、需求资料、Git 源码、HBuilderX 工作目录、服务器部署和已有验证结果整理为下一任务可以直接接续的基线。
>
> 本文生成过程中未修改业务代码、未重新构建 H5/App、未修改服务器业务数据、未推送远程仓库。唯一新增内容是本文件本身。

## 1. 交接结论

当前项目的核心业务实现已经基本完成，核心链条是：

~~~text
独立定制打印入口
  -> 上传模型
  -> 异步文件校验
  -> 选择尺寸/材料/数量
  -> 提交询价
  -> 后台报价和填写预计交付时间
  -> 用户确认报价
  -> 生成定制待支付订单
  -> mock 支付
  -> FIFO 排单
  -> 开始打印/完成打印
  -> 到店取件
  -> 用户确认收货或后台取件码核销
  -> 待评价
  -> 评价完成或 7 天超时自动完成
~~~

普通成品和秒杀商品不进入打印排单：

~~~text
快递：待支付 -> 待发货 -> 待收货 -> 待评价 -> 已完成
普通下单自提：待支付 -> 待核销 -> 待评价 -> 已完成
待发货订单改为到店自取：填写自取地点 -> 待收货 -> 待评价 -> 已完成
~~~

目前不能直接宣布“项目已经正式验收完成”，原因是：

1. 三份 Git 中的需求/改版文档存在若干历史描述没有同步更新。
2. 当前测试用例文档明确标记为“仅完成用例设计，未执行测试”。
3. 以前的真实链路验证和线上部署验证存在，但服务器测试数据后来已清理，尚未用干净数据重新完成正式回归。
4. H5 已经由用户使用 HBuilderX 构建并部署，但 App 尚未构建。
5. 节点 H 的代码和服务器定时任务已经存在，但私有文件目录需要通过一次真实模型上传才能完成运行时验收。
6. PRD 中的管理员登录安全要求（首次强制改密、5 次失败锁定、30 分钟无操作退出等）尚未找到完整的专用实现证据，需要在正式测试前确认是补实现还是移出当前一期范围。

下一任务应优先做文档基线统一和干净数据 Web 联调，不应直接按旧文档继续扩大业务改动。

## 2. 资料和目录的可信顺序

### 2.1 Git 源码主工作树

本次审计使用的 Git 工作树：

~~~text
C:\Users\1\.codex\visualizations\2026\08\31\01a057c6-8183-7ca0-86bc-3994dfda4da1\git-maintenance\3d-shoping
~~~

远程仓库：

~~~text
https://github.com/wxlys/3d-shoping.git
~~~

当前分支和版本：

~~~text
branch: main
HEAD:   ae0915af652885fe89ef65b93394f5d91408e4d7
origin/main: ae0915af652885fe89ef65b93394f5d91408e4d7
~~~

截至本交接审计开始时：

- git status --short --branch：工作树干净，main...origin/main。
- git diff --stat：无差异。
- git diff --name-status：无差异。
- git diff --check HEAD：无输出，退出成功。
- 最近一次业务提交是 ae0915a fix: anchor print services below user navigation。

本文件写入后，预期 Git 状态只增加一个未提交文件 handoff.md；没有其他代码差异。

### 2.2 HBuilderX 工作目录

用户实际用于 HBuilderX 构建的 uni-app 目录：

~~~text
F:\procedure\codex\program\test_project\crmeb-src\template\uni-app
~~~

最新构建目录：

~~~text
F:\procedure\codex\program\test_project\crmeb-src\template\uni-app\unpackage\dist\build\web
~~~

已核对的关键源码文件与 Git 工作树版本 SHA-256 一致：

| 文件 | SHA-256（两处相同） |
|---|---|
| template/uni-app/pages.json | D02BE2FAD0B5FB253254E53C9F5CC7E06896E5EA40805D79AC1F7F7839A8F87B |
| template/uni-app/pages/user/index.vue | 3320D8ACBDBE7A54585CD34970F20D5DED1FA69BF62E8C129DE802DCA690BFF5 |
| template/uni-app/subpackage/diyComponents/pageDesign.vue | A8B72642D9F850F51DEE162BF8E9921A29CDBD5F060DED5F155E7C6DA38CAB37 |

### 2.3 两套 docs 的差异

F:\procedure\codex\program\test_project\docs 不是当前 Git 文档主版本：

- F 目录中的 PRD、改版清单最后修改时间为 2026-09-03。
- F 目录没有当前 Git 中的 3D打印业务收敛重构方案-v0.1.md。
- F 目录文档仍描述“后台定制商品/商品类型”“自动排单估时”等旧方案。
- Git 工作树 docs 才包含 2026-09-06 的 R7.1～R7.3 记录和当前重构方案。

新任务必须以 Git 工作树中的文档和源码为主，以 F 目录仅作为 HBuilderX 源码/构建产物位置；不要把 F 目录旧文档重新当成需求基线。

## 3. 目标与验收标准

### 3.1 业务目标

项目目标是把 CRMEB v6 收敛为：

1. 独立定制 3D 打印服务。
2. 3D 打印成品商城。
3. 成品商品参与的限时秒杀。
4. 管理后台负责商品、订单、询价、排单、打印履约、退款、核销、内容与装修。
5. Web 用户端和 App 共用 uni-app 业务源码。
6. 仅使用 mock 支付；正式支付渠道不在一期范围。
7. 消息以站内信为主，不做 App 推送。

### 3.2 必须保持的业务约束

- 定制打印是独立入口，不是商品类型、不属于商品分类、不需要后台预先创建入口商品。
- 商品后台只管理成品及其 SKU、库存、物流、限购、推荐和秒杀配置。
- 成品和秒杀订单不能进入打印询价或打印队列。
- 同一模型只在旧询价为“待报价/已报价”时阻止重复提交。
- 询价已确认、已取消、已过期，或关联订单已取消/退款后，同一文件可以再次询价。
- 管理员报价时填写金额和预计交付时间；排单不能按材料、体积、填充率等重新覆盖交付时间。
- 调整预计开始时间时，预计交付时间默认等量平移；管理员可以在同一窗口直接覆盖预计交付时间。
- 定制打印只到店自提，使用 6 位取件码。
- 普通下单自提使用 12 位核销码；普通快递不生成核销码。
- 普通待发货订单改为到店自取时必须填写自取地点，保存后进入待收货，不得产生虚拟发货。
- 用户确认收货或后台核销后进入待评价；评价完成或待评价满 7 天才进入已完成。
- 退款审核期间不消耗待评价自动完成时间。
- 定制订单只有排队中且尚未开始打印时可退款；普通成品/秒杀只有待发货或待核销时可退款。
- 装修能力保留。个人中心的“我的询价”“我的文件”固定插入功能导航组之后，推荐标题和商品列表之前；主题组件可单独显示/隐藏，不得把这两个固定入口推到页面底部。

### 3.3 验收出口

业务功能完成的最低出口：

- 源码、SQL/配置、构建产物、服务器部署版本可互相追溯。
- 定制、普通成品、秒杀三条主链路均用干净数据跑通。
- 正常流程、取消/退款、重复操作、越权、文件安全和定时任务均有可重复记录。
- 后台超级管理员、客服、财务、库管的页面/API权限边界有执行记录。
- Web 正式回归完成后再由用户使用 HBuilderX 构建 App。
- App 构建完成后，才能进入自动化、安全、性能和发布收尾。

## 4. 对话历史核对结果

用户指定的文件：

~~~text
C:\Users\1\Desktop\历史对话.md
~~~

本次从该路径以及 C:\Users\1 用户目录递归查找，没有找到该文件。因此下面的历史不是重新读取该 MD 文件，而是根据当前会话中保留的用户消息、Git 提交记录、服务器备份和代码结果整理的；下一任务若能提供原文件，应优先补充对照。

### 4.1 历史要求和已经落地的方向

1. 用户提供 GitHub 仓库和服务器别名 wsrser，要求服务器运行版本作为重要参考。
2. 用户要求后续 H5 构建全部由 HBuilderX 桌面端完成，开发侧只维护源码和部署已构建包；因此不要在自动任务中重复执行 H5 构建。
3. 用户先完成了询价、后台报价、用户确认报价、预期交付时间展示和排单管理。
4. 用户随后发现退款后文件仍不能再次询价、退款状态展示错误，要求按“询价任务完成即可复用文件”的规则修复。
5. 用户明确要求交付时间只由后台手工报价/改期控制，不再根据材料、尺寸、体积自动估时覆盖。
6. 用户连续反馈了确认收货、核销码长度、配送信息空表单、普通商品虚拟发货、到店自取和订单四节点展示问题；之后形成当前的两套核销规则和待评价状态机。
7. 用户要求改版前先审核需求文档，保留装修能力，不要因重构截断后续节点入口或共享能力。
8. 用户要求定制打印独立于普通商品分类和 SKU；首页推荐栏目可使用商品推荐标记，个人中心不应展示占位商品。
9. 用户反馈个人中心中“我的询价/我的文件”被主题组件推到页面底部，要求固定在导航组下方，同时保留主题标题和商品列表的独立显隐功能。
10. 当前用户要求暂停继续实现，先生成完整交接文档，再迁移到新任务。

### 4.2 历史反馈与代码处理关系

| 历史问题 | 当前处理结论 | 交接状态 |
|---|---|---|
| 后台没有询价报价入口 | 增加后台询价列表、详情、报价金额和预计交付时间 | 代码/页面已存在，待干净数据回归 |
| 退款后相同文件不能再次询价 | 允许已确认、已取消、已过期询价或已取消/已退款订单解除文件绑定 | 代码已实现，待干净数据回归 |
| 排单覆盖后台预计交付时间 | 排单仅维护 FIFO/设备/位置；人工时间优先 | 代码已实现，待回归 |
| 定制 6 位取件码与普通 12 位核销码混淆 | 定制取件码和普通自提核销码分开 | 代码已实现，待回归 |
| 普通商品显示虚拟发货 | 普通商品使用快递、配送、到店自取，不使用虚拟业务 | 代码已处理，待回归 |
| “无需配送”无操作内容 | 业务含义改为“到店自取”，地点必填并进入待收货 | 代码已处理，待回归 |
| 确认收货后直接已完成 | 收货/核销进入待评价，评价或 7 天后完成 | 代码已处理，待回归 |
| 评价状态节点缺失/错位 | 定制与普通订单页面恢复四节点/相应状态显示 | 最新 H5 已构建，待页面回归 |
| 个人中心固定入口位置异常 | 插槽绑定到用户导航组之后 | 最新源码和部署包已更新 |

## 5. 已完成事项

### 5.1 需求和数据基础

- 创建打印设备、打印队列、模型文件、询价单等业务表和订单扩展字段。
- 增加打印配置：设备数、营业时间、文件扩展名/大小/数量/保留期、报价有效期、支付超时、评价自动完成天数等。
- 初始化 3D 打印成品分类、首页推荐位和可编辑占位成品。
- 清理服务器旧测试订单、商品、询价、模型文件、排单及相关测试数据；执行前保留数据库和模型文件备份。该动作记录在改版清单中，本次交接没有重复清理。

### 5.2 定制打印和询价

- 定制打印从商品类型和商品详情中拆出，独立进入模型上传/询价页面。
- 用户可上传 STL、OBJ、3MF、STP、STEP 文件，选择 S/M/L/XL、PLA/PETG、数量后提交询价。
- 后台可查看询价文件和参数，填写报价金额与预计交付时间。
- 用户可查看报价、确认报价、生成定制待支付订单，重复确认受到幂等保护。
- 待报价和已报价可取消；确认后询价转订单，不再按询价取消。
- 报价默认 48 小时有效，过期后不能确认。
- 同一文件的复用规则已按当前业务决定实现。

### 5.3 文件安全和节点 H

- 上传后文件状态为“校验中”，由定时维护任务完成 MIME、文件头、大小和文件存在性检查，转换为“已通过/校验失败”。
- 私有文件命名为随机 token，存储根目录是运行时目录下的 runtime/storage/print_models，不在 Web 根目录内。
- 下载使用短时 HMAC 签名地址，代码对路径做 realpath 和目录边界检查。
- 已引用文件不能删除；未引用且超过保留期的文件由定时任务清理。
- 文件校验结果、报价、过期、支付超时、排单、打印完成、退款和自动完成等主要节点接入站内信。

### 5.4 支付、排单、履约和退款

- 定制订单 mock 支付成功后只入队一次，普通成品/秒杀订单不进入打印队列。
- 排单按支付时间 FIFO，按设备维护队列号和排位。
- 后台可开始打印、完成打印、调整预计开始时间、手工覆盖预计交付时间、更新进度备注。
- 完成打印后生成 6 位取件码，订单进入待取。
- 定制订单确认收货/后台 6 位核销后进入待评价。
- 普通商品支持快递、商家配送、普通下单到店自提，以及待发货阶段改为到店自取。
- 普通快递发货前保持待发货，不显示核销区块；普通自提使用 12 位核销码。
- 到店自取必须填写地点，客户端展示地点并允许确认收货。
- 退款审核通过后定制订单移出队列并重算；文件绑定释放；普通成品/秒杀按规则回补库存。
- 退款审核中的待评价订单暂停自动完成计时。

### 5.5 用户端、后台和装修

- 用户端关闭/移除优惠券、积分、签到、会员、分销、砍价、拼团、预售、直播、虚拟商品等当前一期无关入口。
- 装修文件和装修后台能力保留，运行时通过业务组件白名单避免实例化已下线组件。
- 首页栏目按精选商品、首发新品、好物好价、热门推荐处理，分别使用 is_best、is_new、is_benefit、is_hot 标记。
- 商品后台保留成品字段、库存、物流、推荐和秒杀能力；没有定制打印商品类型入口。
- 个人中心保留主题标题和商品列表的显隐能力，但“我的询价”“我的文件”通过 user-services 插槽固定在用户导航组之后。
- 后台工作台、询价管理、排单管理、核销、打印配置和核心 RBAC 入口已收敛。

### 5.6 构建和服务器

- 2026-09-05 修复 pages.json 语法问题并移除评价抽奖残留入口。
- 2026-09-06 用户使用 HBuilderX 构建最新 Web 包。
- 最新构建产物时间：2026-09-06 13:52:54。
- 本地 unpackage/dist/build/web/index.html SHA-256：
  12c3f493260a907210a8f23436be5eda17306c36e862dbe9191aa45b2c2632e4。
- 服务器 /home/wsr/crmeb/crmeb/public/index.html SHA-256 与本地一致。
- 最新 H5 包备份/部署记录：
  /home/wsr/deploy-backups/h5-r7.3-before-20260906-135254/。
- 服务器运行容器可见：Nginx、PHP、MySQL、Redis 等处于运行状态；本次检查时 PHP、MySQL、Redis 容器可用/健康。
- 服务器 Cron 每分钟调用 /api/crontab/run，任务自身依据 eb_system_timer.next_execution_time 控制执行频率。

## 6. 关键决策及理由

### 6.1 定制打印独立于商品体系

决策：定制打印不是商品类型、商品分类或入口 SKU。

理由：询价发生在用户上传模型和选择打印参数之后，价格由后台审核报价决定；如果把它当作普通商品，会引入商品价格、库存、购物车、虚拟发货和普通物流逻辑，正是之前多次出现状态混乱的来源。

### 6.2 只保留成品商品和成品秒杀

决策：商品后台只管理成品；秒杀是成品参与的营销活动，不是第三种商品类型。

理由：保持 CRMEB 购物车、库存和秒杀基础能力，同时把定制打印从普通商品金额/库存/物流状态机中隔离。

### 6.3 排单不再自动估算交付时间

决策：排单仅维护 FIFO、设备、序号、排位和状态；预计交付时间以管理员报价/改期为准。

理由：实际打印时长受模型、机器、人工安排等因素影响，自动公式曾覆盖管理员明确填写的日期，造成用户端时间不一致。保留配置字段是兼容策略，但不得重新参与最终交付时间覆盖。

### 6.4 到店自取替代普通商品“无需配送/虚拟发货”

决策：普通成品待发货时可以设置到店自取，必须填写地点，订单进入待收货。

理由：系统没有虚拟业务；用户仍需到店取货并确认收货，不能使用虚拟发货绕过实际履约状态。

### 6.5 收货后必须待评价

决策：用户确认收货和后台核销均只进入待评价；评价成功或满 7 天未评价后才进入已完成。

理由：需要保留评价业务阶段，避免收货后直接终态导致评价入口和四节点展示不一致。

### 6.6 采用入口/权限/运行时裁剪，暂不大面积物理删除

决策：无关模块先隐藏入口、关闭写入、收敛路由/权限和运行时组件，保留共享表和底层适配代码。

理由：CRMEB 核心订单服务、H5/App 共用代码和历史数据存在耦合；过早物理删除可能截断后续 App、文件、支付、权限和秒杀测试。

### 6.7 H5 构建由用户执行

决策：开发侧维护 uni-app 源码，H5 由用户在 HBuilderX 构建；构建包由开发侧部署和备份。

理由：用户明确要求避免重复构建造成额度消耗，并且 HBuilderX 是当前实际构建环境。

## 7. 重要文件和位置

以下路径均相对于 Git 工作树
C:\Users\1\.codex\visualizations\2026\08\31\01a057c6-8183-7ca0-86bc-3994dfda4da1\git-maintenance\3d-shoping。

| 文件/目录 | 作用和重点位置 |
|---|---|
| docs/3D打印服务系统-PRD-v0.1.md | 业务需求、状态机、文件、退款、排单和权限要求；需先修订历史矛盾 |
| docs/改版清单-v0.1.md | 节点执行记录和下一步任务；第 8、53、105 行附近状态过期 |
| docs/3D打印业务收敛重构方案-v0.1.md | 重构边界、保护清单、R1～R6；第 8、50、54、203、210、244、248 行附近有旧描述 |
| docs/已完成节点测试用例-v0.1.md | 当前已完成节点用例；第 7 行明确“仅完成用例设计，未执行测试” |
| docs/测试计划-v0.1.md | 测试计划、进入/退出标准和自动化计划；测试数据回滚方案仍待定 |
| docs/sql/3dprint-stage1.sql | 第一阶段表、字段和配置基础 |
| docs/sql/3dprint-stage5-inquiry.sql | 询价相关表/字段 |
| docs/sql/3dprint-stage7-nodeH.sql | 节点 H 文件、定时任务、消息和配置脚本 |
| docs/sql/3dprint-refactor-r1-catalog.sql | 3D 打印成品分类初始化 |
| docs/sql/3dprint-refactor-r7-home.sql | 首页推荐、导航和占位商品初始化 |
| crmeb-src/app/services/print3d/PrintInquiryServices.php | 上传、异步校验、询价、报价、文件复用、签名下载；上传/校验约在 53、211 行，复用约在 281、379 行，私有存储约在 1100 行 |
| crmeb-src/app/services/printqueue/PrintQueueServices.php | 入队、开始/完成打印、改排期、取消、自动完成；改排期约 206 行，自动完成约 306 行 |
| crmeb-src/app/services/system/crontab/CrontabRunServices.php | printMaintenance()，约 263 行，串联文件校验、报价过期、自动完成和文件清理 |
| crmeb-src/app/api/controller/v1/print3d/Inquiry.php | 用户上传、文件列表/下载、询价、取消、确认接口 |
| crmeb-src/app/api/route/v1.php | 签名下载和询价路由；公共签名下载约 27 行，询价路由约 247～251 行 |
| crmeb-src/app/services/order/StoreOrderTakeServices.php | 用户确认收货状态和 take_delivery 记录 |
| crmeb-src/app/services/order/StoreOrderWriteOffServices.php | 普通订单/定制订单核销分流和核销码规则 |
| crmeb-src/app/services/order/StoreOrderDeliveryServices.php | 快递、配送、到店自取发货和地点保存 |
| crmeb-src/template/uni-app/pages/goods/order_details/index.vue | 用户订单状态、取件码、自提地点、待评价四节点展示 |
| crmeb-src/template/uni-app/pages/print/inquiry/index.vue | 独立定制询价页 |
| crmeb-src/template/uni-app/pages/print/inquiry_list/index.vue | 我的询价 |
| crmeb-src/template/uni-app/pages/print/files/index.vue | 我的文件、复用和删除 |
| crmeb-src/template/uni-app/pages/user/index.vue | 个人中心，user-services 插槽约 36～51 行 |
| crmeb-src/template/uni-app/subpackage/diyComponents/pageDesign.vue | 用户导航插槽约 194～197 行，业务组件白名单约 463～490 行 |
| crmeb-src/template/uni-app/pages/goods_cate/goods_cate.vue | 分类页中的独立定制打印入口 |
| crmeb-src/template/admin/src/pages/order/inquiry/index.vue | 后台询价列表、报价和预计交付时间 |
| crmeb-src/template/admin/src/pages/order/printQueue/index.vue | 后台排单、改期和手工预计交付时间 |
| crmeb-src/template/admin/src/pages/order/orderList/handle/orderSend.vue | 普通订单快递、配送、到店自取发货 |
| crmeb-src/template/admin/src/pages/product/productAdd/components/BusinessSetting.vue | 成品推荐标记：精选、首发、好物好价、热门 |
| crmeb-src/template/uni-app/pages.json | 用户端路由；当前 path 注册项计数约 80，不是重构文档写的约 130 |
| deploy-crmeb/docker-compose.yml | 本地/服务器 Docker 运行结构参考 |
| deploy-crmeb/crontab.example | Cron 配置参考 |
| F:\procedure\codex\program\test_project\.codex-deploy | 本地保存的 H5/backend 构建和部署备份 |

## 8. Git 节点和变更历程

以下节点均已经在 main 并推送到 origin/main：

| 提交 | 时间 | 作用 |
|---|---|---|
| e66593c | 2026-09-04 10:06 | 记录早期 H5 构建部署完成 |
| 7b45216 | 2026-09-04 10:38 | 修复普通订单到店自取与确认收货 |
| 1d06329 | 2026-09-04 11:56 | 校正需求边界并固化 UTF-8 SQL |
| a33dae8 | 2026-09-04 12:40 | 制定业务收敛重构方案 |
| 8dbaea6 | 2026-09-04 12:52 | 补充后续节点保护边界 |
| 3cf4b1c | 2026-09-04 20:06 | R1.1 初始化 3D 打印成品分类 |
| 0fcecb8 | 2026-09-04 20:12 | R1.2 收敛商品编辑字段 |
| f20268c | 2026-09-04 20:23 | R1.3 收敛首页装修与业务入口 |
| 4b84071 | 2026-09-04 20:25 | R2.1 收敛后台菜单入口 |
| 2ed4b5a | 2026-09-04 20:31 | R2.2 调整后台工作台入口 |
| e13b2fd | 2026-09-04 20:49 | 部署用户构建的 H5 |
| 3f8dacc | 2026-09-04 21:26 | R2.3 后台权限和打印工作台 |
| 7e8533a | 2026-09-04 21:51 | H1 文件异步校验和私有存储 |
| a827111 | 2026-09-04 23:26 | R3.2 收敛用户端路由和支付入口 |
| 7adfbeb | 2026-09-05 09:31 | R4.1 收敛新订单后端业务边界 |
| 6f5dc42 | 2026-09-05 09:38 | R5.1 复核打印/普通订单边界 |
| c932bc7 | 2026-09-05 23:38 | 修复 pages.json 尾随逗号 |
| 23fd90e | 2026-09-05 23:43 | 移除评价抽奖残留入口 |
| 8196c98 | 2026-09-05 23:50 | 记录 R6.3 Web 构建部署节点 |
| 27881c2 | 2026-09-06 12:26 | R7.1 定制打印从商品体系独立化 |
| 67988e6 | 2026-09-06 12:40 | R7.2 首页推荐和业务导航校正 |
| 43c9bc0 | 2026-09-06 12:45 | 记录 R7 首页部署 |
| 2ad6609 | 2026-09-06 12:47 | 增加首页业务导航图标 |
| b8e0f70 | 2026-09-06 12:48 | 记录 R7 业务对齐 |
| 585c55b | 2026-09-06 13:08 | 个人中心不显示商品推荐 |
| ae0915a | 2026-09-06 13:22 | 把我的询价/我的文件固定到用户导航下方 |

## 9. 已执行命令和结果

### 9.1 Git 和文件审计

执行过的主要命令：

~~~powershell
git status --short --branch
git log --oneline --decorate -15
git log --date=iso --pretty=format:'%h | %ad | %an | %s' -25
git remote -v
git diff --stat
git diff --name-status
git worktree list --porcelain
git diff --check HEAD
git diff --check origin/main
~~~

结果：

- 远程 fetch：https://github.com/wxlys/3d-shoping.git。
- 远程 push：git@github.com:wxlys/3d-shoping.git。
- HEAD 和 origin/main 都是 ae0915af...。
- 审计开始时无未提交代码差异。
- Git worktree 只有当前主工作树。
- Git whitespace 检查通过。

### 9.2 文档和源码搜索

执行过的主要审计命令：

~~~powershell
rg --files
rg -n 'R7|R6|H5|App|节点 H|定制打印|商品分类|当前状态|进行中|等待' docs
rg -n 'printMaintenance|autoReceipt|print_models|signedDownload|verifyDownloadSignature' crmeb-src/app crmeb-src/template
rg -n 'is_print|queue_status|take_delivery|verify_code|delivery_type|expected_deliver_at' crmeb-src/app crmeb-src/template
rg -n 'must_change_password|login.*lock|idle|timeout.*logout|首次登录|强制改密' crmeb-src/app crmeb-src/template/admin/src
~~~

结果：

- 发现节点 H 的代码和节点 I 的共用基础仍在。
- 发现 PRD/改版清单/重构方案的过期描述，具体见第 11 节。
- 发现管理员登录增强要求缺少明确专用实现证据，需单独确认。
- pages.json 当前路径注册项计数为 80，重构方案中的“约 130”属于历史数值。

### 9.3 服务器只读检查

执行过的主要命令：

~~~powershell
ssh -o BatchMode=yes -o ConnectTimeout=10 wsrser "date '+%Y-%m-%d %H:%M:%S %z'"
ssh wsrser "docker ps --format '{{.Names}} {{.Image}} {{.Status}}'"
ssh wsrser "sha256sum /home/wsr/crmeb/crmeb/public/index.html"
ssh wsrser "crontab -l"
ssh wsrser "stat -c '%y %s bytes' /home/wsr/crmeb/crmeb/runtime/.timer"
ssh wsrser "find /home/wsr/crmeb -type d -name 'print_models'"
~~~

结果：

- SSH 别名 wsrser 可用，服务器时间为 2026-09-06 14:39:20 +0800。
- crmeb3d-nginx、crmeb3d-php、crmeb3d-mysql、crmeb3d-redis 等容器运行中；MySQL 和 Redis 显示 healthy。
- 服务器 H5 入口文件大小 783 bytes，时间 2026-09-06 13:52:54。
- 服务器 H5 入口 SHA-256 为 12c3f493260a907210a8f23436be5eda17306c36e862dbe9191aa45b2c2632e4。
- Cron 为每分钟调用：
  * * * * * /usr/bin/flock -n /tmp/crmeb-crontab.lock /usr/bin/curl --fail --silent --show-error --max-time 20 http://127.0.0.1:18080/api/crontab/run >/dev/null 2>&1
- .timer 文件存在，检查时约 10 bytes，说明定时执行器有心跳。
- 当前没有找到 runtime/storage/print_models 目录；这是因为本次没有重新上传模型文件，不能据此判定代码错误。下一步必须通过真实上传触发目录创建并验证。
- 服务器最新 H5 备份目录为 /home/wsr/deploy-backups/h5-r7.3-before-20260906-135254/。

### 9.4 HTTP 和静态检查

执行过的主要命令：

~~~powershell
Invoke-WebRequest http://8.148.5.169:18080/
Invoke-WebRequest http://8.148.5.169:18080/api/crontab/run
node --check crmeb-src/template/uni-app/api/print3d.js
node --check crmeb-src/template/uni-app/utils/request.js
ssh wsrser "docker exec crmeb3d-php php -l <server-file>"
~~~

结果：

- 主站 / 返回 HTTP 200。
- /api/crontab/run 返回 HTTP 200；该接口会触发一次维护入口/更新定时心跳，后续不要把它当作纯静态 GET 反复调用。
- /api/ 和 /api/public 探测返回 404，属于无效探测路径，不代表主站或业务路由失败。
- Node 两个 JS 文件语法检查通过。
- 服务器 PHP 7.4.33 对以下 10 个关键文件执行 php -l 均通过：
  PrintInquiryServices.php、PrintQueueServices.php、CrontabRunServices.php、用户/后台询价控制器、Cron 控制器、收货服务、核销服务、排单控制器和配送控制器。

## 10. 测试结果和证据边界

### 10.1 已有成功证据

- 用户提供的 HBuilderX 日志证明最新 Web 版本成功导出：
  2026-09-06 13:52:54 项目 uni-app 导出Web成功。
- 本地构建产物和服务器 index.html 的 SHA-256 一致。
- 线上主路由返回 200。
- PHP 关键服务在服务器 PHP 7.4.33 下语法检查通过。
- Node 关键 JS 语法检查通过。
- 过往对话记录和改版清单记录了询价→报价→确认→mock支付→排单的真实链路，但这些数据已被后续清理，不能代替当前干净数据回归。

### 10.2 尚无成功证据的项目

以下不能写成“已通过”，只能写成“代码存在/待验证”：

- 干净数据库下完整定制打印正向链路。
- 干净数据库下普通成品快递、普通下单自提、待发货改到店自取三条普通履约链路。
- 秒杀库存、限购、支付超时和并发正确性。
- 真实模型上传后异步校验通过/失败、私有目录创建、签名下载和清理。
- 用户 A/B 越权访问询价和文件。
- 客服、财务、库管角色的页面和接口边界。
- 评价 7 天自动完成及退款审核暂停计时。
- App 构建和 App 端上传/支付/路由。
- 自动化接口、UI、性能和安全报告。

### 10.3 测试文档状态

docs/已完成节点测试用例-v0.1.md 第 7 行写明：当前状态为“仅完成用例设计，未执行测试”。

docs/测试计划-v0.1.md 仍是测试计划，不是执行报告；其中测试数据脚本化和数据库回滚方案仍有待定内容。

截至本次审计，没有发现可追溯的正式执行报告、缺陷统计、Allure 报告、性能报告或安全测试报告。因此不能把“代码静态检查通过”误写成“业务测试通过”。

## 11. 三份需求文档与当前实现的偏差

### 11.1 核心业务已经基本统一的部分

以下内容在当前代码、Git 改版记录和 Git 文档中基本一致：

- 定制打印独立入口和独立询价流程。
- 成品商品、秒杀和定制打印的业务边界。
- 询价状态、报价金额、预计交付时间和确认转订单。
- 重复文件复用规则。
- FIFO 排单和人工预计交付时间。
- 定制 6 位取件码、普通自提 12 位核销码。
- 普通订单快递/配送/到店自取分流。
- 收货/核销后进入待评价，评价或 7 天后完成。
- 首页推荐标记和装修能力保留。
- 个人中心固定入口布局。

### 11.2 必须修订的 Git 文档内容

#### PRD docs/3D打印服务系统-PRD-v0.1.md

- 第 7 行仍写“等待 H5 构建联调”，应改为“最新 H5 已构建部署，待干净数据 Web 联调”。
- 第 42 行仍写当前实现边界“截至 2026-09-04”，应更新审计日期。
- 第 49 行把节点 H 的异步文件状态、Web 根目录外存储、文件消息和 RBAC 写成未完成，与代码、改版清单和服务器部署记录冲突。
- 第 136 行仍写上传请求内同步校验和节点 H 未完成，与当前 PrintInquiryServices 的异步状态和私有存储实现冲突。
- 第 212 行仍写商品业务类型为“定制/成品”，应改为商品只管理成品，product_type 仅历史兼容字段。
- 第 225 行仍把定制上传入口写在商品详情，应改为独立定制打印入口。
- 第 230 行个人中心需要补充“我的询价”和固定入口位置。
- 第 209 行管理员登录安全要求需要确认实际实现；在没有实现或验证前不得标成已完成。
- 支付范围应明确为微信/支付宝 mock；线下支付、余额等历史兼容代码即使保留，当前也不应作为业务入口。

#### 改版清单 docs/改版清单-v0.1.md

- 第 8 行仍写等待最新 H5 构建，应改为 H5 已构建部署、待 Web 联调、App 构建后置。
- 第 53 行仍写 R7 等待用户构建 H5，与 2026-09-06 13:52:54 构建结果冲突。
- 第 105 行节点 H 标为“进行中”，但第 107～112 行各项已经全部写为已完成；建议改为“功能完成，待正式验收”。
- 第 145 行“客服可选保留”与重构方案和当前保留的客服入口口径不明确，建议明确为保留或移出一期。
- 第 97 行及历史条目可以保留为历史记录，但应和当前最新部署状态区分。

#### 重构方案 docs/3D打印业务收敛重构方案-v0.1.md

- 第 8 行仍写等待 H5 构建联调。
- 第 50 行在说明“定制打印不是商品分类”后又写“可用定制打印服务商品承载入口”，与 R7.1 决策矛盾，应删除后半句。
- 第 54 行仍写服务器存在 35 条旧商城演示分类，属于 R1 已执行前的历史描述，应改为当前初始化结果。
- 第 203 行仍写“新建 3D 打印商品分类”，与定制服务独立化相冲突；只能初始化成品分类。
- 第 210 行仍写商品编辑展示“成品/定制”字段，应改为只展示成品字段。
- 第 244 行仍写等待用户构建 H5，应更新为最新构建已完成。
- 第 248 行只记录 R6 旧 Web 构建，应补充 R7.3 最新部署和 SHA/备份信息。
- 第 142 行的 pages.json“约 130 个页面”与当前路径项约 80 个不一致，应改成实际审计值或删除这个易过期的数量。

## 12. 未完成清单

### P0：下一任务必须先处理

1. 更新三份 Git 需求文档，使“定制打印独立入口”“商品只管理成品”“R7/H5 当前状态”“节点 H 状态”完全一致。
2. 明确管理员登录安全要求：补实现并测试，或正式记录为延期/非当前验收范围。
3. 用干净数据执行定制打印完整链路。
4. 用干净数据执行普通成品快递、自提、待发货改到店自取三条链路。
5. 用真实模型文件验证异步校验、私有目录、签名下载、删除限制和清理任务。
6. 验证退款状态、文件绑定释放、排单重算和重复询价规则。
7. 验证所有收货/核销路径统一进入待评价，而不是直接已完成。

### P1：P0 稳定后处理

1. 验收客服、财务、库管和超级管理员的页面/API权限矩阵。
2. 验证消息中心的报价、过期、支付超时、排单、打印完成、退款和自动完成通知。
3. 验证 48 小时报价过期、15 分钟支付超时、7 天自动完成等配置可修改且生效。
4. 验证秒杀库存原子扣减、活动时间、每人限购和支付超时释放。
5. 清理剩余旧文案和文档中的旧“定制商品/虚拟发货/自动估时”表述。

### P2：发布和测试收尾

1. 用户在 HBuilderX 构建 App。
2. 执行 App 登录、上传、询价、支付、订单、页面跳转和弱网场景。
3. 补接口自动化、文件安全、越权、XSS/SQL 注入和支付幂等测试。
4. 执行秒杀 50 并发、排单 100 单重算等性能场景，记录实际环境和结果。
5. 补 GitHub Actions、缺陷记录、需求跟踪矩阵、测试报告和发布标签。

## 13. 已知问题和风险

1. **文档基线分裂**：F 目录的文档是旧快照；必须以 Git 工作树 docs 为主，完成 R7.4 文档修订后再冻结测试基线。
2. **私有目录尚未被真实上传创建**：代码使用 runtime/storage/print_models，服务器当前未发现该目录；需要一次真实上传验证目录权限、文件名和下载。
3. **管理员安全要求未闭环**：PRD要求和源码检索结果之间缺少完整证据，不可直接当作已实现。
4. **旧兼容代码仍在仓库**：积分、虚拟、线下支付等旧文件可能仍物理存在，这是当前“先关闭入口和写入、暂不删共用代码”的策略；验收重点是不可见、不可写、不可通过旧路由越权调用，而不是仓库中完全没有文件。
5. **当前 pages.json 仍是 uni-app JSONC 风格**：它包含注释，不能用普通 JSON 解析器直接作为标准 JSON 判断；HBuilderX 构建成功才是本项目实际编译证据。
6. **正式业务测试未执行**：历史“真实链路通过”不能代替清空数据后的新一轮测试。
7. **服务器环境与本地 HBuilder 目录是两套位置**：修改源码后必须确认 HBuilderX 源码、Git 工作树、构建产物和服务器部署包的版本一致。

## 14. 不可重复的失败尝试和历史缺陷

### 14.1 已解决的 H5 构建失败

用户在 2026-09-05 23:37:31 构建时遇到：

~~~text
pages.json: line 932, col 18, Unexpected comma.
pages.json: line 990, col 18, Unexpected comma.
项目 uni-app 编译失败。
~~~

对应修复提交是 c932bc7 R6.1 修复Web页面配置。用户随后于 23:40:22 构建成功并导出 Web。后续如果再次出现相同错误，先检查 pages.json JSONC 条件编译区块的逗号，不要重复做无关业务修改。

### 14.2 已解决的履约缺陷

以下问题来自历史联调，不代表当前已经完成干净数据回归：

- 普通成品被误标为虚拟发货。
- 普通订单确认收货出现 Indirect modification of overloaded element of app\\model\\order\\StoreOrder has no effect。
- 普通订单配送信息只有订单号、无法配置。
- “无需配送”没有操作内容且前后端状态不一致。
- 定制订单核销要求 12 位但用户只看到 6 位。
- 收货后订单节点直接显示已完成，待评价状态丢失。
- 退款后显示“退款中”或文件仍被旧询价绑定。

这些问题分别由 7b45216、后续履约/状态修复和 7e8533a 等节点处理；新任务必须通过第 15 节的干净数据回归确认没有复发。

### 14.3 本次审计中的无效命令

审计服务器 Cron 时曾误把 PowerShell 的 Select-Object 放进远程 Linux 命令，服务器返回：

~~~text
bash: Select-Object: command not found
~~~

随后已用 Linux tail -20 重新检查，Cron 配置结果正常。这是交接审计命令误用，不是项目失败，不要据此判断服务器任务异常。

### 14.4 不可获取的历史附件

桌面上的 历史对话.md 未找到，下一任务不能声称已经逐字核对该附件。如果用户重新提供，应将其作为补充证据并检查是否有比当前 Git 文档更晚的业务决定。

## 15. 下一步具体动作

### 动作 1：先完成 R7.4 文档基线统一

只修改以下三个 Git 文档，不改业务代码：

~~~text
docs/3D打印服务系统-PRD-v0.1.md
docs/改版清单-v0.1.md
docs/3D打印业务收敛重构方案-v0.1.md
~~~

统一内容：

- 定制打印独立于商品体系。
- 商品后台只管理成品。
- 最新 H5 已于 2026-09-06 13:52:54 构建并部署。
- 节点 H 改为“代码和任务完成，待运行时验收”。
- 个人中心固定入口和装修显隐规则。
- 普通快递/自提/到店自取和两种核销码规则。
- 将登录安全要求明确标为已实现、待验证或延期。

文档修改后单独提交 Git，并重新检查 git diff --check。

### 动作 2：先做 Web 干净数据冒烟，不新增业务范围

按以下顺序执行：

1. 首页：定制打印入口、商品分类、推荐栏目、秒杀入口。
2. 个人中心：装修标题/商品列表分别显示和隐藏时，检查“我的询价/我的文件”始终在导航组下方。
3. 定制询价：上传合法/非法文件，观察异步状态、失败原因和私有下载。
4. 后台报价：金额、预计交付时间、报价有效期、用户端展示。
5. 用户确认报价、mock 支付、排单、改期、开始/完成打印、取件码。
6. 用户确认收货/后台核销、待评价、评价后完成。
7. 普通成品：快递、自提、待发货改到店自取、地点修改、确认收货。
8. 退款、取消、重复文件、支付超时、重复确认和重复核销。
9. 服务器定时任务和文件清理。

每组发现阻断问题后，先记录环境、订单号、请求、数据库状态和截图，再决定是否需要代码修复。

### 动作 3：每个修复节点保持可回滚

每次修改按以下顺序：

1. 记录当前 Git commit、数据库备份和 H5 备份。
2. 只处理一个明确问题。
3. 运行静态检查和相关链路验证。
4. 提交独立 Git 节点并推送 GitHub。
5. 用户重新构建 H5 后再部署，保留服务器旧包。
6. 更新改版清单、测试记录和本交接文档状态。

### 动作 4：Web 稳定后再做 App

由用户在 HBuilderX 构建 App。开发侧负责：

- 核对 App 使用的源码 commit。
- 核对 API 地址、App 配置和上传权限。
- 部署/保留 App 发布包和版本信息。
- 只做 App 端适配修复，不重新改变已冻结的 Web 业务规则。

### 动作 5：最后进入用户主导的正式测试

用户此前明确表示测试计划和用例由用户编写/执行。开发侧在测试期间只负责：

- 根据缺陷复现结果修复。
- 保持 Git/数据库/H5 备份。
- 更新需求跟踪矩阵、缺陷和已知问题。
- 不把静态检查、HTTP 200 或历史链路记录冒充完整测试通过。

## 16. 新任务接续检查表

新任务开始时先执行：

~~~powershell
Set-Location 'C:\Users\1\.codex\visualizations\2026\08\31\01a057c6-8183-7ca0-86bc-3994dfda4da1\git-maintenance\3d-shoping'
git status --short --branch
git log -1 --oneline --decorate
git remote -v
~~~

然后确认：

- 是否存在本文件以及是否有后续任务新增的提交。
- HBuilderX 工作目录是否仍为 F:\procedure\codex\program\test_project\crmeb-src\template\uni-app。
- 用户是否重新提供了 历史对话.md。
- 三份 Git 文档是否已经完成 R7.4 统一。
- 是否已经有干净数据联调报告，而不是只看历史提交说明。
- 服务器最新 H5 包和 Git 源码是否对应同一个节点。

交接完成点：下一任务能够仅凭本文件、Git 工作树和服务器只读信息，知道当前实现边界、不能误用的旧文档、尚未验证的风险和应该执行的第一步。

## 17. 2026-09-06 MakerWorld 商品初始化

- 新增 `docs/sql/3dprint-refactor-r8-makerworld-products.sql`，按 SPU `MW26090600001`～`MW26090600008` 幂等创建 8 个 MakerWorld 成品。
- 8 个一级分类均已覆盖：家居生活、玩具与游戏、工具与配件、模型与摆件、创意礼品、教育与创客、3D打印耗材、其他成品；每个商品均有子分类、默认 SKU、详情和来源链接。
- 先执行 R1 分类初始化，再执行 R8 商品脚本。R7 四个占位商品仅下架（`is_show=0`），未删除。
- 服务器数据库备份：`/home/wsr/deploy-backups/db-r8-makerworld-before-20260906/crmeb.sql.gz`。
- 服务器当前容器内 API（`http://127.0.0.1:18080`）已核对 8 个商品、8 个 SKU、8 个详情和 8 条商品分类关联；脚本重复执行后仍保持各 1 条商品。
- `https://test.bz.wuht.net` 的外部入口仍返回旧分类/旧商品数据，疑似指向另一套入口或缓存；本次未将其当作新容器 API 的验证依据。

## 18. 2026-09-07 联调增量状态

### 18.1 定制订单状态与排期（已实现并部署，待用户页面联调）

- 订单管理新增“定制待支付、定制排队中、定制制作中、定制待取件”独立区块，普通待支付/待发货/待核销/待收货筛选排除定制订单。
- 新增定制订单类型筛选、定制排期列和订单列表“调整排期”入口；预计交付时间必须晚于预计开始打印时间，前端、控制器和服务层均有校验。
- 自动重排遇到交付时间冲突时不再生成倒置排期；无效排期会提示先调整，并阻止开始打印。
- 管理后台生产构建成功（保留项目原有 11 条 CSS 顺序警告）；线上 `public/admin/index.html` SHA-256 为 `c609ea8ecc7817a79c47f37a969b84911337435b04cdea370aac493cf684718a`。
- 服务器备份：`/home/wsr/deploy-backups/custom-order-status-schedule-20260907-1010/`。

### 18.2 秒杀未开始时“单独购买”误扣秒杀限额（调查中）

- 期望规则：单独购买必须按普通商品下单，不携带 `seckill_id`，不扣秒杀 `quota`；只有秒杀购买入口才占用秒杀额度。
- 已完成第一阶段：读取当前工作区和相关 diff，确认本轮已有秒杀物流继承与订单配送校验改动，尚未修改本缺陷代码。
- 下一步：沿秒杀详情页单独购买参数、购物车 `type`、订单确认返回的 `seckill_id`、支付/库存回写链路逐层定位，并在每个重要阶段更新本节。
- 第二阶段结论：秒杀详情“单独购买”会跳转普通商品详情，普通详情的 `cart/add` 请求未主动携带 `secKillId`；但订单创建服务仍信任客户端回传的 `seckillId`，存在普通购物车被污染后误扣秒杀限额的服务端缺口。
- 线上只读证据：普通订单 `cp619833683674660864` 的 `seckill_id=0`；活动 2 当前 `quota_show=5, quota=4, sales=1`，对应更早的真实秒杀订单 `cp619650039987306496`（`seckill_id=2`），现有数据未显示该普通订单实际扣过活动限额。
- 修复方向：订单活动类型与库存扣减只认服务端缓存购物车中的 `type/seckill_id`，忽略客户端订单提交参数；普通购物车一律走普通库存。
- 第三阶段已实现：`StoreOrderCreateServices::createOrder()` 会先清空客户端 `seckillId`，仅当缓存购物车 `type=1` 且存在有效 `seckill_id` 时才创建秒杀订单；`decGoodsStock()` 也逐项读取可信购物车标识，`type=0` 始终只扣普通商品库存。
- 静态检查：`git diff --check` 通过（只有工作树既有的 CRLF 提示）；待完成服务器 PHP lint、部署和线上只读回归。
- 第四阶段已部署：服务器暂存和正式文件 PHP lint 均通过，SHA-256 均为 `64cb39dafefd9ddc2c55dbc002cb195976e88b3a8174d3354487e4035a557906`；PHP 容器重启后运行正常，API HTTP 200。
- 服务器回滚备份：`/home/wsr/deploy-backups/seckill-alone-stock-20260907-1030/StoreOrderCreateServices.php`。
- 本修复仅涉及 PHP 后端，不需要重新构建 H5；仍需由用户执行一次“未开始秒杀页 → 单独购买 → 创建订单”的页面回归，核对新订单 `seckill_id=0` 且活动 `quota/sales` 不变。

### 18.3 到店取件统一六位取件码（修复中）

- 最新业务规则：凡 `shipping_type=2` 的到店取件订单，无论定制打印或普通成品，都使用六位数字取件码；用户端不允许自行“确认收货”，必须由后台输入该订单取件码并核销。
- 第一阶段根因已确认：订单列表行内“立即核销”调用 `write_update/:order_id`，后端会自行读取订单现有 `verify_code` 并直接完成核销，完全绕过人工输入；用户确认收货服务仍为打印订单保留了直接收货分支；普通到店订单仍生成十二位核销码。
- 修复边界：普通到店订单改为生成唯一六位码；行内核销弹出输入框并在服务端校验输入码与所选订单完全一致；通用核销框只接受六位码；用户端隐藏到店订单确认收货入口，同时后端确认收货接口拒绝所有到店订单。
- 下一步：完成后台、服务端和 uni-app 三端修改，执行 PHP/前端静态检查与管理后台构建；后端和管理后台部署后，再由用户使用 HBuilderX 构建并部署 H5 以生效客户端按钮变更。
- 第二阶段已实现：新增普通到店订单六位唯一取件码生成方法，新建自提订单和礼物改自提均改用六位码；订单行“立即核销”改为打开取件码输入框，并由后端同时校验六位格式、所选订单号和取件码一致性；用户确认收货接口拒绝所有自提/定制订单，订单详情及订单列表商品组件隐藏到店订单“确认收货”。
- 兼容边界：同城配送仍保留原十二位核销码和通用“订单核销”入口，不受本次到店取件规则影响；存量十二位自提待核销订单需在部署时迁移为新的六位码。
- 下一步：运行 PHP lint、管理后台生产构建和差异检查；随后备份线上文件及数据库，迁移存量待核销自提码并部署后端/管理后台。uni-app 源码变更需用户用 HBuilderX 重新构建 H5。
- 第三阶段验证与部署完成：`git diff --check` 通过；管理后台 `npm run build` 成功（仅保留项目既有 11 条 CSS 顺序警告）；暂存文件和线上文件均通过 PHP lint，本地/线上五个 PHP 文件 SHA-256 一致；PHP 容器重启正常，站点首页和管理后台 HTTP 200。
- 线上已备份并部署，回滚目录为 `/home/wsr/deploy-backups/pickup-code-verification-20260907-104445/`，包含原后端文件、原管理后台静态文件和 `eb_store_order` 表备份；新管理后台 `index.html` SHA-256 为 `31a23d48cb422d3c03c5719b943c584720985e0992297d0658b219e1af83aee8`。
- 存量迁移完成：普通自提订单记录 ID 14、16 的取件码已由十二位替换为六位；复核范围已扩大到所有未取消、未完成的自提订单（含未支付订单），其中非六位记录数为 0；未在文档记录实际取件码明文。
- HBuilderX 工作目录与 Git 工作区不是同一物理文件映射，已对 `F:\procedure\codex\program\test_project\crmeb-src\template\uni-app` 中的订单详情和订单商品组件同步做最小修改，两个对应文件与 Git 工作区版本哈希一致。
- 第四阶段 H5 已构建并部署：用户于 2026-09-07 10:53:03 使用 HBuilderX 导出 Web；构建包确认包含 `shipping_type=2`/定制订单不渲染“确认收货”的条件。仅替换线上 `public/assets`、`public/pages`、`public/static`、`public/index.html`，未动后台、上传文件和其他服务文件。
- H5 回滚备份：`/home/wsr/deploy-backups/h5-pickup-code-before-20260907-110333/`；本地与线上 `index.html` SHA-256 均为 `703a8f0e1ab986018cdfce815828e6b8aa31d9215c977c10511d68ee490f1325`，订单详情脚本 SHA-256 均为 `8f70adb1e4105294d6a5f94a7b14304eee021684253a1520d86ca621e5e466d7`。
- 线上验证：公网 `/index.html`、订单详情新脚本及 SPA 订单详情路由均 HTTP 200；站点根路径返回原有桌面 iframe 包装页，iframe 指向 SPA 路由，属于既有入口结构。
- 当前剩余动作：用一笔定制待取订单和一笔普通自提订单做页面联调；用户端应只显示六位取件码且没有“确认收货”，后台输入错误码应拒绝，正确码应核销并进入待评价。

### 18.4 到店订单用户端状态归类（修复中）

- 最新规则：所有到店自取订单（普通、秒杀、定制）在用户订单详情中跳过“待发货”，直接处于“待收货”，并显示取件码；配送订单仍保留待发货节点。
- 根因：`StoreOrderServices::getUserOrderByKey()` 在门店自提开关关闭时把已有 `shipping_type=2` 改写为 1，随后清空 `verify_code`；用户端列表的 `status=2` 查询只查数据库 `status=2`，而普通自提待核销是 `status=0`、定制打印完成待取是 `status=1 + queue_status=3`。
- 已实现未部署：详情保留真实配送方式；普通自提待核销和定制待取映射 `_status._type=2`（待收货）；取件码对普通自提 `status=0` 可见；用户端待发货/待收货列表增加专用查询，待发货仅保留配送订单，待收货包含配送已发货、普通自提待核销、定制待取；计数同步调整；详情时间轴对自提隐藏待发货并显示待收货。
- 线上证据：订单 `cp619835376621584384`（记录 ID 16）为 `is_print=1, queue_status=0, paid=1, status=0, shipping_type=2`，取件码长度为 6；当前系统已有的定制待取记录包括 ID 17 等。
- 已完成后端部署：仅替换 `StoreOrderServices.php`、`StoreOrderDao.php`，部署前备份为 `/home/wsr/deploy-backups/pickup-order-status-before-20260907-114253/`；暂存文件和线上文件 PHP lint 均通过，线上 SHA-256 分别为 `dfd52ee5b6467b0ed0a6af787850f2a17dcB92D1550BF341A4DA8826554C7429` 与 `b4a2c1721c1cd078592120fc492f563816D6D191786519DFAAF9352624D9D566`（实际服务器输出为全小写同值），API 未登录请求返回预期 401。
- 本次后端逻辑：用户待发货仅保留配送订单；待收货包含配送已发货、普通/秒杀自提未核销、定制打印完成待取；订单详情保留真实 `shipping_type`，自提订单直接映射为待收货并显示六位取件码。
- 当前剩余动作：用户需再次用 HBuilderX 构建并部署包含订单详情时间轴改动的 H5；随后分别验证配送、普通自提、秒杀自提、定制待取四种状态，确认自提不出现待发货且能看到取件码。
- 追加前端修正：自提订单进度图改为独立四节点“待付款→待收货→待评价→已完成”，避免复用配送五节点导致待收货图标不点亮；Git 工作区与 HBuilderX 源码哈希一致。该改动尚未进入线上 H5，需重新构建部署。
- 第五阶段 H5 已构建并部署：用户于 2026-09-07 11:55:43 使用 HBuilderX 导出 Web；构建包确认包含自提四节点时间轴。线上备份为 `/home/wsr/deploy-backups/h5-self-pickup-before-20260907-121048/`，仅替换 `public/assets`、`public/pages`、`public/static`、`public/index.html`。
- 本次线上校验：本地与线上 `index.html` SHA-256 均为 `49355234ef76374189346d18fb2700e7fe30f606e3c92649032fd97f2f8afd89`；订单详情脚本 SHA-256 均为 `c8e0506dd6c0344d0035cb77836ae5a27172734a67c6a3273ec10789de6d03e3`；内网和公网首页/订单详情脚本均 HTTP 200。
- 当前剩余动作：做用户端真实联调（配送订单、普通自提、秒杀自提、定制待取），确认自提订单详情不显示待发货、显示六位取件码且无确认收货按钮；后台输入正确取件码后应核销，错误码应拒绝。

### 18.5 秒杀退款后重新购买（已实现并部署）

- 根因：秒杀复用了通用 `getBuyCount()`，其中未支付订单和已退款订单的 `total_num` 也会计入用户限购数量，导致退款后仍被判定为达到限购。
- 修复规则：新增 `getSuccessfulBuyCount()`，只统计 `paid=1`、`refund_status=0` 且 `pid!=-1` 的有效成功交易数量；未支付订单、已退款订单、拆分后废弃的父订单均不占用秒杀限购名额。被拒绝退款且恢复为 `refund_status=0` 的成功订单仍正常计入。拼团/预售继续使用原通用计数逻辑。
- 线上只读数据验证：当前秒杀订单中，已退款订单 `cp619834995711672320`（`seckill_id=3, refund_status=2`）不进入有效计数；有效计数 SQL 结果为 `uid=1,seckill_id=2,count=1`、`uid=1,seckill_id=5,count=1`。
- 已部署文件：`app/dao/order/StoreOrderDao.php`、`app/services/activity/seckill/StoreSeckillServices.php`；备份目录 `/home/wsr/deploy-backups/seckill-limit-refund-before-20260907-173945/`。
- 校验结果：暂存/线上 PHP lint 通过，API HTTP 200；本地与线上 SHA-256 分别为 `fe11609fd61d2739ac9642a5c3acd61262fefe9b6ab2db18ab68be8c123e9d74`、`feba48cb4c5c2bdc2c2589cbd564440c55d3cb96729125655d881c2d0826dcd1`；`git diff --check` 通过。该修复只涉及后端，不需要重新构建 H5。
- 待用户回归：同一用户购买秒杀商品后完成退款，再次进入同一秒杀活动购买一件，应允许下单；未退款的有效成功购买仍受原限购数量限制。

### 18.6 用户订单待发货/待收货分类计数（已实现并部署）

- 用户反馈：待发货角标显示 5，但实际配送待发货只有 2；共有 7 个未收货订单，期望其中 5 个到店自取/定制订单直接归入待收货。
- 根因：`StoreOrderServices::getOrderData()` 的角标曾按数据库 `status=1/2` 统计，而用户列表接口按配送方式和定制队列分类；同时 `StoreOrderDao::getUserOrderApiList()` 的待收货条件只覆盖定制 `queue_status=3`，遗漏定制队列 1/2 及普通/秒杀自提的其他有效状态。
- 修复：抽取 `getUserOrderApiQuery()`，列表和计数共用同一条件；待发货仅统计已支付、未退款、未取消、非定制的配送订单（`shipping_type=1,status=0`）；待收货统计配送已发货订单，以及普通/秒杀自提未核销订单（`shipping_type=2,status in(0,1)`）和定制自提队列 1/2/3。计数查询增加 `getUserOrderApiCount()`，并限定父订单 `pid=0`。
- 当前用户真实数据复核：配送待发货为订单记录 ID 21、19，共 2；普通/秒杀自提为 ID 23、22、20，共 3；定制自提队列 1 为 ID 25、24，共 2；因此待收货共 5、未收货合计 7。
- 已部署文件：`app/dao/order/StoreOrderDao.php`、`app/services/order/StoreOrderServices.php`；部署备份目录 `/home/wsr/deploy-backups/user-order-buckets-before-20260907-174817/`。
- 校验结果：暂存/线上 PHP lint 通过；本地与线上 SHA-256 分别为 `320a4ce8c9e20f05228a546beaab88a4bb68ef652217285f6f1917025c8c35ca`、`221d24f80d6604dfd4a26756f083b8b6c3a6ba081927977d6adeb6b168c215ff`；PHP 容器重启后 API HTTP 200；暂存目录已清理。该修复仅涉及后端，不需要重新构建 H5。
- 待用户回归：刷新/重新登录用户端订单页，确认角标为待发货 2、待收货 5；分别检查配送待发货、普通/秒杀自提、定制排队中/制作中四类订单。若页面仍显示旧角标，先清理客户端缓存后重新请求订单列表接口。

### 18.7 订单角标为 0 的计数接口修复（已实现并部署）

- 用户回归发现：订单列表能够显示订单，但“全部/待付款/待发货/待收货/待评价”角标全部为 0。
- 根因：`StoreOrderDao::getUserOrderApiQuery()` 使用了当前 ThinkPHP 版本不存在的 `orWhere()`，计数调用实际抛出 `method not exist: think\\db\\Query->orWhere`，前端未显示错误而按缺省值 0 渲染。
- 修复：将两个条件分支改为框架支持的 `whereOr()`；列表和计数继续共用同一查询条件。
- 线上探针验证（用户 UID 1）：`unshipped=2`、`received=5`；完整 `getOrderData(1)` 返回 `order_count=17, unpaid=0, unshipped=2, received=5`。
- 已部署文件：`app/dao/order/StoreOrderDao.php`；备份目录 `/home/wsr/deploy-backups/order-count-fix-before-20260907-180109/`。线上文件 SHA-256：`471697a67afa9f9aef72f3c6951ed0087f1f761c5df0ff1a6f9d0046fa40e839`。
- 暂存/线上 PHP lint 通过，PHP 容器重启后接口 HTTP 200，探针和临时目录已清理；本次仅后端修复，不需要重新构建 H5。
- 待用户回归：刷新订单页或重新登录，角标应显示实际数量；若仍为旧值，清理 H5 缓存后重新请求 `/api/order/data`。

### 18.8 个人中心外层订单角标口径统一（已实现并部署）

- 用户反馈：订单页内部数量已正常，但个人中心外层“订单中心”菜单角标仍异常。
- 根因：个人中心调用 `/theme_info/user`，`PublicController::themeInfo()` 原来另行按数据库 `status` 统计菜单数量，未使用订单页已经修复的配送/自提/定制履约阶段分类。
- 修复：个人中心菜单角标改为直接复用 `StoreOrderServices::getOrderData($uid)` 的 `order_count`、`unpaid_count`、`unshipped_count`、`received_count`、`evaluated_count`、`complete_count`；退款角标保持原退款服务统计。
- 已部署文件：`app/api/controller/v1/PublicController.php`；备份目录 `/home/wsr/deploy-backups/personal-center-count-fix-before-20260907-181014/`；线上 SHA-256：`efe504b71a1a453bf930435f0928139728661bc59aace28a23af2034de0aed1d`。
- 暂存/线上 PHP lint 通过，PHP 容器重启后 `/api/theme_info/user` HTTP 200；本次仅后端修复，不需要重新构建 H5。
- 待用户回归：重新进入个人中心或下拉刷新，外层角标应与订单页一致（当前用户预期待发货 2、待收货 5）。

### 18.9 定制打印上传文件名显示不完整（已实现并部署）

- 用户反馈：定制打印上传文件在客户端/后台显示时只剩扩展名（例如 `.3mf`）。线上只读查询确认 `eb_print_file.filename` 确实存在 `.3mf`、以及 `_???.3mf` 这类历史异常值，不是单纯的 CSS 截断。
- 根因：H5/uni-app 通过临时文件路径上传时，服务端收到的 `getOriginalName()` 可能只有扩展名；前端原上传请求没有把 `uni.chooseFile` 返回的真实 `name` 传给接口。
- 修复：`PrintInquiryServices::uploadFile()` 接收 `original_name` 表单字段并优先保存客户端文件名，原生客户端仍回退到框架提供的原始名；`formatFile()` 对历史只有扩展名或乱码占位名返回可读兜底名（例如 `模型文件.3mf`），不改写数据库原始记录；uni-app 询价上传页在 `uni.uploadFile` 中传递 `original_name`。
- 已同步文件：Git 工作区的 `crmeb-src/template/uni-app/pages/print/inquiry/index.vue` 与 HBuilderX 工作目录 `F:\procedure\codex\program\test_project\crmeb-src\template\uni-app\pages\print\inquiry\index.vue` SHA-256 一致（`262d438874384c2de81f56a1ed0c31cef26b0dccd8d1e5c3dad12915f21083c4`）。
- 已部署后端：服务器备份 `/home/wsr/deploy-backups/print-filename-fix-before-20260907-181651/`；线上 `PrintInquiryServices.php` SHA-256 `10a9ec74905283d656ed182ae02fb308ad5841cb4375fda9cabd982c10df7fb0`；暂存与线上 PHP lint 均通过，PHP 容器重启后运行正常。
- 线上探针：`formatFile(['filename'=>'.3mf','ext'=>'3mf'])` 返回 `模型文件.3mf`；合法 `desk-tray.3mf` 保持原名；`/api/print/file/list` 未登录请求返回预期 HTTP 200/业务 401。
- H5 已由用户于 2026-09-07 18:51:25 使用 HBuilderX 导出并部署；线上备份 `/home/wsr/deploy-backups/h5-print-filename-before-20260907-190335/`，仅替换 `public/assets`、`public/pages`、`public/static`、`public/index.html`。
- 本地导出包与线上文件一致：`index.html` SHA-256 `1daf23291ee1351cf4381cfbfb56b51121380e78f2df396ea9118c28265bb13e`；定制打印询价脚本 `pages-print-inquiry-index.b1230c7d.js` SHA-256 `143a97287c4d86c6d0b4fa9603c3663ff4a78e56122f24188b858c1ae659ad55`，脚本包含 `original_name`；首页、询价脚本及 SPA 询价路由均 HTTP 200。
- 当前剩余动作：用户清理 H5 缓存后重新上传一个带中文或英文完整名称的 `3mf/stl/obj/stp/step` 文件，确认用户端文件列表、询价详情和后台打印队列均显示完整文件名；历史数据库中原名已丢失的记录只能显示可读兜底名。
