# CRMEB v6 → 3D 打印服务系统 改版清单 v0.1

| 项目 | 内容 |
|---|---|
| 依据 | PRD v0.1、测试计划 v0.1 |
| 基座 | CRMEB v6（PHP + uni-app，Apache-2.0），已在服务器部署 |
| 原则 | 复用为主、裁剪为辅、只新增必须的自定义模块；不删代码，用菜单/权限/开关裁剪 |
| 当前状态 | 阶段 1～6 主体功能已开发；H5 询价、支付、排单、履约链路已走通，正在完成 H5 基础能力收尾 |

## 当前进度（2026-09-01）

- ✅ 阶段 1 数据库：已新建 eb_print_device / eb_print_queue / eb_print_file / eb_inquiry 四张表。
- ✅ 订单表已扩展 9 个字段（is_print、print_file_id、size_level、material、expected_start_at、expected_deliver_at、queue_status、progress_note、inquiry_id）。
- ✅ 打印参数 21 项已写入系统配置（营业时间、设备数、调试时长、体积/速度/材料系数、超时/有效期/自动收货、文件限制）。
- ✅ 默认设备已初始化（拓竹 A1 × 1）。
- ✅ 阶段 2 支付 mock：新增 `pay_mock_enabled` 配置开关，开启后支付接口直接调用 `paySuccess` 标记支付成功（支付方式记录为 `mock`），不再依赖微信/支付宝回调。
- ✅ 阶段 3 商品类型（后端）：商品表新增 `product_type`（0成品/1定制打印）；后台保存支持该字段；商品列表/详情接口已透出。
- ✅ 阶段 3 商品类型（前端源码）：后台商品编辑页新增“业务类型”选择（成品/定制打印）；H5/App 商品详情按类型分支，定制打印可进入询价页面。
- ✅ 阶段 3 商品类型（后台部署）：管理后台已重新构建并部署到服务器（旧包保留备份），线上后台已出现“业务类型”选择。
- ✅ 阶段 4 节点 A：支付成功后定制订单自动入队（`queue_status=1` + 排单记录），成品/秒杀直接进入待取并生成 6 位取件码；已做事务内运行验证。
- ✅ 阶段 4 节点 B：排单时间计算与重算（营业时间 9:00–21:00、30 分钟调试、体积×填充÷流速×效率；跨天自动顺延；新单入队/取消后重算）；已做双订单运行验证。
- ✅ 阶段 4 节点 C：管理员打印流转（开始打印/打印完成/改排期/进度备注）、待取生成取件码与 `pickup_at`、自动收货（`auto_receipt_days` + 定时任务 + 手动触发接口）；已做全流程事务验证。
- ✅ 阶段 4 节点 D：订单详情接口透出排位、预计开始/交付时间、进度备注、取件码；H5 订单详情展示块已构建并部署。
- ✅ 阶段 5 询价主体：用户上传模型、选择尺寸/材料/数量、提交询价、查看询价及确认报价；后台询价列表、详情、报价/作废接口和页面已开发并部署。
- ✅ 后台排单管理：排单列表、开始打印、完成打印、调整排期、更新进度已开发并部署。
- ✅ 管理后台乱码问题已修复并重新构建部署；本次乱码修复未涉及 H5 构建。
- ✅ 真实链路已走通：管理员提交报价后，用户已确认报价并完成 mock 支付，定制订单已自动进入排单，客户端可看到价格和预计交付时间。
- ✅ H5 履约与退款规则补齐：打印完成显示待取/取件码并可确认收货；定制仅排队且未开打可退款，成品/秒杀仅待取可退款；退款通过后移出队列并重算。
- ✅ H5“我的文件”已补齐：个人中心和询价记录均可进入，已通过文件可复用询价，未被业务引用的文件可删除。
- ✅ 退款后文件可再次询价：退款成功、询价取消或报价过期后解除旧业务绑定；历史绑定数据在再次提交时兼容释放。
- ✅ 站内消息已接入询价报价/过期、支付入队、开始打印、排期调整、进度更新、打印完成待取和自动完成。
- ✅ 周期维护任务已落地：合并处理报价过期、待取自动完成和未引用文件清理；替换服务器原有无对应执行方法的旧任务配置。
- ⏳ 当前节点：继续完成 H5 剩余改版与部署；正式测试和 App 构建按用户要求延后到 H5 全部完成之后。

## 当前核心业务链条

| 步骤 | 操作角色 | 业务动作 | 目标状态 | 当前情况 |
|---|---|---|---|---|
| 1 | 用户 | 搜索后台上架的定制商品，进入商品详情并点击“定制询价” | 进入询价页 | 已实现，真实链路已通过 |
| 2 | 用户 | 上传模型，选择尺寸档位、材料、数量并提交 | 询价单=待报价 | 已实现，真实链路已通过 |
| 3 | 管理员 | 后台“订单 → 询价单管理”查看文件和参数，填写报价 | 询价单=已报价，写入 48 小时有效期 | 已实现，真实链路已通过 |
| 4 | 用户 | 在“我的询价”查看报价并确认 | 询价单=已确认，生成待支付定制订单 | 已实现，真实链路已通过 |
| 5 | 用户 | 使用 mock 支付完成付款 | 定制订单=排队中并写入排单 | 已实现，真实链路已通过 |
| 6 | 管理员 | 在“排单管理”调整排期、开始打印、更新进度、完成打印 | 排队中 → 制作中 → 待取 | 已实现、待真实联调 |
| 7 | 用户 | 查看排位、预计交付时间和取件码，确认收货 | 待取 → 已完成 | 主体已实现、待定时任务与回归 |

## 后续节点与任务（按优先级执行）

### 节点 E：询价闭环联调与需求补齐（P0，主体完成）

- 用服务器现有待报价记录完成一次真实链路：后台报价 → 用户查看报价 → 用户确认 → 生成待支付订单。
- 核对后台入口稳定显示；验证退出重登、强制刷新和菜单缓存升级后均能看到“询价单管理/排单管理”。
- 按 PRD 补齐“管理员报价金额 + 预计交付时间”；当前页面和接口只有报价金额。
- 按 PRD 统一取消规则：待报价和已报价是否都允许用户取消；当前实现只允许待报价取消。
- 验证报价金额边界、48 小时有效期、重复报价/重复确认幂等、过期后禁止确认、用户只能访问自己的询价和文件。
- 验证模型下载地址、文件状态、尺寸/材料/数量在前后台显示一致。

### 节点 F：询价转订单、支付与自动排单闭环（P0，主体完成）

- 验证确认报价后生成的订单金额、用户、文件、规格、数量、询价单关联关系完全正确。
- 验证 mock 支付成功、重复支付回调幂等，以及定制订单支付后只入队一次。
- 验证 FIFO 排单、营业时间跨天、调试时长、材料/尺寸系数、预计开始/交付时间和多订单重算。
- 补齐并验证支付超时自动取消：取消待支付订单、释放相关资源，且不得产生排单记录。
- 在用户订单详情和后台订单详情同时核对排位、预计时间、进度备注及关联模型。

### 节点 G：打印履约、退款与取件闭环（P0，功能已补齐，待后续统一测试）

- 验证后台开始打印、完成打印、调整排期、更新进度的状态限制和重复操作保护。
- 验证打印完成后生成 6 位取件码，并在用户端进入“待取”状态正确展示。
- 验证用户确认收货和待取满 7 天自动完成，退款审核中必须暂停自动收货。
- 按订单类型落实退款限制：定制仅排队中且未开打印可申请；成品/秒杀仅待取可申请。
- 验证退款通过后定制订单移出队列并重算后续排期；退款拒绝后恢复原状态；mock 退款金额正确。
- ✅ 修复退款完成后打印订单状态覆盖问题：退款状态优先于排单取消状态，并释放模型文件绑定。

### 节点 H：后台任务、消息与基础能力补齐（P1，进行中）

- 修正并启动 CRMEB 定时任务执行链，验证报价过期、支付超时、自动收货和未引用文件清理四类任务真实运行。
- 接入站内信：文件校验、询价报价/过期、排单变化、打印完成、待取、退款结果、支付超时和催付。
- 将文件处理补齐为明确的“校验中 → 通过/失败”状态；完善 MIME、文件头、大小、数量、伪造扩展名和存储安全测试。
- ✅ 增加“我的文件”复用入口及删除/保留期规则；已被询价/订单引用的文件禁止删除。
- 补齐设备数量、营业时间、打印参数的后台配置入口，并验证修改参数后排单重算行为。
- 完成一期菜单裁剪和 RBAC：超级管理员、客服、财务、库管按 PRD 权限矩阵验收。

### 节点 I：三端回归、自动化与发布收尾（P0/P1，H5 改版完成后开展）

- 执行 H5 核心流程回归并完成 App 构建；App 与 H5 共用业务逻辑，但分别验证上传、支付和页面跳转。
- 建立 P0 全链路用例：登录、商品、定制询价、报价、转单、支付、排单、打印、待取、退款。
- 增加接口自动化、文件上传安全、水平/垂直越权、XSS/SQL 注入、支付幂等测试。
- 完成 50 并发秒杀、100 单排单重算性能测试，并记录环境和结果。
- 建立 GitHub Actions 基础流水线，沉淀 RTM、缺陷记录、已知问题、测试报告和版本发布记录。

### 每个节点的完成标准

- 源码、SQL/配置、前端构建、服务器部署四者版本一致。
- 正向流程、异常流程、权限与幂等场景均有可重复验证记录。
- 改版清单和测试用例同步更新，并形成独立 Git 节点后再进入下一阶段。

---

## 一、复用（保留不改）

- 用户注册/登录：手机号+验证码、账号密码双通道。
- 商品基础：分类、商品、规格属性、SKU、库存、上下架、批量操作。
- 秒杀基础模块：活动时间、秒杀价、库存、倒计时。
- 订单基础：下单、支付状态、取消、退款申请。
- 用户中心：资料、我的订单、消息中心（站内信）。
- 后台：管理员、角色权限（RBAC）、操作日志、系统设置、文件上传、财务流水。
- 前端框架：管理后台 Vue2、用户端 uni-app（H5/App 同源）。

## 二、裁剪（一期隐藏/关闭，不删代码）

- 营销：拼团、砍价、优惠券、积分商城、分销/佣金、会员等级、付费会员。
- 资金：用户充值、余额提现（余额支付可保留或关闭）。
- 履约：快递发货/物流配送（配送方式锁定为“到店自提”）、门店核销动作（取件码仅展示，用户手动确认收货）。
- 其他：直播、客服（可选保留）、PC 端（可选保留）、分享（可选）、短信/微信模板消息依赖（改站内信）。
- 用户收货地址管理（自提不需要）。
- App 推送（一期不做，消息走站内信）。

## 三、改造（改现有）

### 1. 商品类型

- 类型扩展：成品、秒杀、定制打印（新增）。
- 定制打印：无库存、按单生产；规格组改为“尺寸档位 + 材料”。
- 尺寸档位：S=100、M=500、L=2000、XL=4000 cm³（标准体积，可配）。
- 材料：PLA、PETG（系数 1.0 / 0.71，可配）。

### 2. 秒杀

- 复用现有秒杀活动/商品配置，关闭其他营销。
- 确认/补齐：下单锁库存、每人限购、支付超时释放库存。
- 秒杀商品直接购买，不进购物车。

### 3. 订单状态机（核心改造）

- 定制：待支付 → 排队中 → 制作中 → 待取 → 已完成；另有已取消、退款审核中、已退款。
- 成品/秒杀：待支付 → 待取 → 已完成（不排队不制作）。
- 待取状态生成 6 位数字取件码。
- 确认收货：用户点击确认，或待取满 7 天自动完成（退款审核中暂停计时）。
- 退款限制：定制仅“排队中（开始打印前）”可申请；成品/秒杀仅“待取”可申请；完成后不可退。
- 订单详情增加：预计开始/交付时间、当前排位、前方数量、进度备注、取件码。

### 4. 支付（mock）

- 关闭微信/支付宝真实配置，替换为 mock 支付成功回调（幂等）。
- 支付超时默认 15 分钟，超时自动取消并释放库存/移除队列。

### 5. 消息通知（站内信）

- 触发点：文件校验结果、询价报价/过期、排单变化、打印完成、进入待取、退款结果、支付超时提醒。

### 6. 文件上传

- 上传上限调整为 100MB（PHP 与 CRMEB 配置同步）。
- 新增服务端校验：后缀白名单（stl/obj/3mf/stp/step）+ MIME + 文件头。
- 存储安全：随机文件名、Web 根目录外、uploads 目录禁止执行脚本。

### 7. 定时任务

- 支付超时取消（15 分钟）。
- 报价过期（48 小时）。
- 自动收货（7 天）。
- 过期文件清理（30 天，未被引用）。

## 四、新增（自定义开发）

### 4.1 数据表

| 表 | 关键字段 |
|---|---|
| eb_print_device | 名称、型号（A1）、构建体积、状态、营业开始/结束、设备调试时长 |
| eb_print_queue | 订单号、设备、队列序号、预计开始/结束、实际开始/结束、状态、调整人/时间 |
| eb_print_file | 用户、文件名、存储名、扩展名、大小、状态（校验中/通过/失败）、失败原因 |
| eb_inquiry | 用户、文件、尺寸档位、材料、数量、状态（待报价/已报价/已确认/已过期/已取消）、报价金额/人/时间、有效期、转订单号 |
| order 扩展字段 | is_print、pickup_code、expected_start_at、expected_deliver_at、queue_status、progress_note、file_id、size_level、material |
| system_config 扩展 | 营业时间、调试时长、设备数量、速度/填充/效率系数、档位体积、材料系数、支付超时、报价有效期、自动收货天数、文件大小/数量/保留期 |

### 4.2 后端服务/接口

- 文件校验服务（上传后异步校验）。
- 询价单服务：提交 → 报价 → 确认转正式订单 → 过期/取消。
- 排单服务：支付成功入队、预计时间计算、取消/退款/改排期后重算、通知。
- 订单扩展：取件码生成、进度备注、确认收货、自动完成。
- 打印参数配置接口。

### 4.3 管理后台（Vue2）

- 新增“询价单管理”：列表、详情、报价、转单、过期处理。
- 新增“排单管理”：队列列表、调整排期、设备与参数配置。
- 商品编辑支持“定制打印”类型与尺寸/材料规格。
- 订单管理增加定制字段、进度备注、取件码展示。
- 菜单权限注册；隐藏裁剪模块入口。

### 4.4 用户端（H5/uni-app，App 同源）

- 定制流程：上传文件 → 校验状态 → 选尺寸/材料/数量 → 提交询价 → 查看报价 → 确认 → 支付。
- 我的询价单：报价确认、过期提示。
- 我的文件：仅“通过”状态可复用下单。
- 订单详情：排位、预计交付、进度、取件码、确认收货、退款申请。
- 购物车仅成品；秒杀直接购买；隐藏营销/分销/充值入口。

## 五、初始化数据

- 设备：拓竹 A1 × 1（256×256×256 mm）。
- 参数：营业 09:00–21:00、调试 30 分钟/单、打印效率 0.6、填充 0.2、速度系数按材料、支付超时 15 分钟、报价有效期 48 小时、自动收货 7 天、文件上限 100MB/50 个/保留 30 天。
- 角色与菜单权限。
- 测试数据：定制入口商品、成品商品、秒杀商品。

## 六、实施顺序与工作量（单人估算）

| 阶段 | 内容 | 工期 |
|---|---|---|
| 1 | 数据表 + 参数配置 + 商品类型改造 + 订单状态机 + 支付 mock | 3–4 天 |
| 2 | 文件校验 + 询价单 + 转正式订单 | 3–4 天 |
| 3 | 排单计算/队列/改排期/通知 | 3 天 |
| 4 | 管理后台页面 + 用户端 H5/App 页面 | 4–5 天 |
| 5 | 定时任务、菜单裁剪、初始化数据、联调回归 | 2–3 天 |

合计约 15–18 个工作日，与 6 周时间盒匹配；测试文档工作（用例/RTM）可并行。

## 七、风险与注意

- 订单状态机改造影响面大：先冻结当前代码版本，每步小验证，避免改崩支付/退款链路。
- CRMEB 要求保留版权标识，改版代码继续保留原作者版权声明。
- 秒杀并发依赖 Redis，确认现有扣库存与限购实现后再压测。
- uni-app App 端构建需要 HBuilderX，排期预留。
- 定时任务依赖 CRMEB 的队列/定时组件，需确认可用后接入。
